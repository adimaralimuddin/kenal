import React from "react";
import NotifItemDiv from "./NotifItemDiv";

const NotifItemFollow = ({ notif }) => {
  return (
    <div>
      <NotifItemDiv notif={notif} />
    </div>
  );
};

export default NotifItemFollow;
