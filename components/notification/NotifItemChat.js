import React from "react";
import NotifItemDiv from "./NotifItemDiv";

const NotifItemChat = ({ notif }) => {
  return (
    <div>
      <NotifItemDiv notif={notif} />
    </div>
  );
};

export default NotifItemChat;
